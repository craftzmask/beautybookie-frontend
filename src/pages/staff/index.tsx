import Layout from "@/components/layout";
import * as React from "react";

interface IStaffProps {}

const Staff: React.FunctionComponent<IStaffProps> = (props) => {
  return (
    <Layout>
      <div>Staff</div>
    </Layout>
  );
};

export default Staff;